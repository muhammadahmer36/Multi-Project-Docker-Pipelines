export enum DropDownCondition {
     // eslint-disable-next-line no-unused-vars
    allEnabled = 0,
     // eslint-disable-next-line no-unused-vars
    timesheetEnabled = 1,
     // eslint-disable-next-line no-unused-vars
    timesheetDisabled = 2,
     // eslint-disable-next-line no-unused-vars
    employeeEnabled = 3,
     // eslint-disable-next-line no-unused-vars
    employeeDisabled = 4,
}

// eslint-disable-next-line no-unused-vars
export enum SelectedItem {
    // eslint-disable-next-line no-unused-vars
   atleastOneItemSelected = 1,
    // eslint-disable-next-line no-unused-vars
   noneIsSelected = 0
}
