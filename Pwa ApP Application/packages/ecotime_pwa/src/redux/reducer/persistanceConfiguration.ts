import hardSet from 'reduxjs-toolkit-persist/lib/stateReconciler/hardSet';
import storage from 'reduxjs-toolkit-persist/lib/storage';

export const persistConfigOfflineQueue = {
  key: 'offlineQueue',
  storage,
  hardSet,
};

export const persistConnectivityStatusBar = {
  key: 'persistConnectivityStatusBar',
  storage,
  hardSet,
};

export const persistPermissions = {
  key: 'persistPermissions',
  storage,
  hardSet,
};

export const persistConfigTimeComponent = {
  key: 'persistConfigTimeComponent',
  storage,
  hardSet,
};

export const persistConfigDashboard = {
  key: 'dashboard',
  storage,
  hardSet,
};

export const persistConfigAdditionalInformation = {
  key: 'additionalInformation',
  storage,
  hardSet,
};

export const persistConfigRunningClockSyncing = {
  key: 'runningClockTimeSyncing',
  storage,
  hardSet,
};

export const persistConfigUniqueIdentifier = {
  key: 'common',
  storage,
  hardSet,
};

export const persistGeolocation = {
  key: 'geolocation',
  storage,
  hardSet,
};

export const persistTimeOff = {
  key: 'timeOff',
  storage,
  hardSet,
};

export const persistTimesheet = {
  key: 'timehseet',
  storage,
  hardSet,
};

export const persistTimesheetWeekly = {
  key: 'timehseetWeekly',
  storage,
  hardSet,
};

export const persistTimesheetDaily = {
  key: 'persistTimesheetDaily',
  storage,
  hardSet,
};

export const persistTimesheetCertifyUncertify = {
  key: 'persistTimesheetCertifyUncertify',
  storage,
  hardSet,
};

export const persistTimesheetNotes = {
  key: 'persistTimesheetNotes',
  storage,
  hardSet,
};

export const persistTimesheetSearchManager = {
  key: 'persistTimesheetSearchManager',
  storage,
  hardSet,
};

export const persistTimesheetManager = {
  key: 'persistTimesheetManager',
  storage,
  hardSet,
};

export const persistBalances = {
  key: 'balances',
  storage,
  hardSet,
};
